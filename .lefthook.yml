pre-push:
  parallel: true
  commands:
    golangci-lint:
      glob: "*.go"
      run: golangci-lint run {staged_files}
    lint-staged:
      root: "peerprep/"
      glob: "*"
      run: npx lint-staged
